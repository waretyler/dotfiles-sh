#compdef pj

_pj () {
  local state

  _arguments \
    '1: :->project'

  case $state in
    (project) _arguments "1:projects:($( echo $(cd ~/Projects/project-list && ls -1 *.sh | sed "s/\.sh$//") ))"
  esac 
}

_pj "$@"

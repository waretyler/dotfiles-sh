#compdef pjo

_pjo () {
  local state

  _arguments \
    '1: :->project'

  case $state in
    (project) _arguments "1:projects:($( echo $(cd ~/Projects/project-list/links && ls -1 )))"
  esac 
}

_pjo "$@"
